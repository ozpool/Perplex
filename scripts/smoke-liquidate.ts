/**
 * Phase 4 deliverable. Land alongside the Rust liquidator + LiquidationEngine.
 * Until then this is a no-op so CI / Makefile targets do not break.
 */
console.log("smoke-liquidate: liquidator arrives in Phase 4 — skipping for now");
process.exit(0);
