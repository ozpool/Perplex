/**
 * Phase 3 deliverable. Land alongside the Rust matching engine.
 * Until then this is a no-op so CI / Makefile targets do not break.
 */
console.log("smoke-trade: matching engine arrives in Phase 3 — skipping for now");
process.exit(0);
